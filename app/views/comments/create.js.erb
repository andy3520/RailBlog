<% if @error.nil? %>
  block = $('#post_number_<%= @comment.post_id %>').closest('.card').find('.new-comment-js');
  $("<%= escape_javascript(render partial: 'comment', locals: { comment: @comment }) %>").appendTo(block);
<% else %>

<% end %>
